{
  "name": "baby-log",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "test:watch": "vitest",
    "cf:dev": "wrangler dev",
    "cf:dev:local": "wrangler dev --config wrangler.local.toml",
    "cf:config:remote-readonly": "node scripts/create-remote-readonly-config.mjs",
    "cf:dev:remote-readonly": "npm run cf:config:remote-readonly && wrangler dev --config wrangler.remote-readonly.toml --port 8788",
    "cf:deploy": "npm run cf:deploy:generated",
    "cf:deploy:generated": "node scripts/write-wrangler-config.mjs && wrangler deploy --config wrangler.generated.toml",
    "cf:deploy:local": "wrangler deploy --config wrangler.local.toml",
    "cf:upload": "node scripts/write-wrangler-config.mjs && wrangler versions upload --config wrangler.generated.toml --tag public-repo-migration --message \"baby-log public repo upload-only verification\" --preview-alias baby-log-public",
    "cf:upload:generated": "node scripts/write-wrangler-config.mjs && wrangler versions upload --config wrangler.generated.toml --tag public-repo-migration --message \"baby-log public repo upload-only verification\" --preview-alias baby-log-public",
    "cf:upload:local": "wrangler versions upload --config wrangler.local.toml --tag public-repo-migration --message \"baby-log public repo upload-only verification\" --preview-alias baby-log-public",
    "d1:migrate:local": "wrangler d1 migrations apply baby_log --local",
    "d1:migrate:remote": "wrangler d1 migrations apply baby_log --remote",
    "smoke:remote-readonly": "node scripts/remote-readonly-smoke.mjs",
    "review:zip": "scripts/create-review-zip.sh",
    "chatgpt:export": "node scripts/chatgpt-source-pack.mjs"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260424.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.1.0",
    "wrangler": "^4.13.0"
  }
}
